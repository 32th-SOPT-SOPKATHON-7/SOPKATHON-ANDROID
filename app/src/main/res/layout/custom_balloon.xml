<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/marker_iv"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/ic_map_marker"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="centerInside"
        android:textColor="@color/white_100"
        android:textSize="12dp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="@id/marker_iv"
        app:layout_constraintEnd_toEndOf="@id/marker_iv"
        app:layout_constraintStart_toStartOf="@id/marker_iv"
        app:layout_constraintBottom_toBottomOf="@id/marker_iv"
        android:layout_marginBottom="4dp"
        tools:text="20" />

</androidx.constraintlayout.widget.ConstraintLayout>